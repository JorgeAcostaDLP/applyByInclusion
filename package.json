{
  "name": "simple-node-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "seedTestData": "rm -f *test && sequelize-cli db:migrate && sequelize-cli db:seed:all",
    "setup": "node createDB.js npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all",
    "test": "NODE_ENV=test npm run seedTestData && npx jest",
    "lint": "eslint .",
    "start": "NODE_ENV=production npm run setup && node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "http-server": "^0.12.1",
    "mysql2": "^2.1.0",
    "sequelize": "^5.21.3",
    "sequelize-cli": "^5.5.1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "nodemon": "^2.0.2",
    "jest": "^25.1.0"
  },
  "engines": {
    "node": "10.x"
  }
}
